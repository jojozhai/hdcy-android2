<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Toolbar_TextAppearance_White">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
    <style name="tab_appearance">
        <item name="android:textSize">16sp</item>
<!--
        <item name="android:colorBackground"></item>
-->
    </style>
    <!--圆角button的特性-->
    <style name="common_button_style">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">6.0dip</item>
        <item name="android:textColor">@android:color/white</item>
<!--
        <item name="android:textSize">@dimen/main_font_body</item>
-->
    </style>

    <style name="EditText_Base">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:cursorVisible">true</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textCursorDrawable">@null</item>
        <item name="android:background">@null</item>
    </style>

    <!-- 开关按钮 -->
    <style name="ToggleButton_Base">
        <item name="offBorderColor">@color/main_line</item>
        <item name="offColor">@color/white</item>
        <item name="onColor">@color/blue</item>
        <item name="sborderWidth">@dimen/line_width</item>
    </style>

</resources>
